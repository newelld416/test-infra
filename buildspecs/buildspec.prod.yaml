version: 0.2
env:
    variables:
        CACHE_CONTROL: "86400"
        BUILD_ENV: "prod"
        S3_BUCKET: "amer-admin-preprod"
        CLOUDFRONT_ID: "E3O6BVQ8M0L2LO"
phases:
    install:
        commands:
            - echo Installing source NPM dependencies...
            - npm install
            - npm install -g @angular/cli
    build:
        commands:
            # Builds Angular application.
            - echo Build started on `date`
            - npm run build
    post_build:
         commands:
            # Clear S3 bucket.
            - aws s3 rm s3://amer-admin-preprod --recursive
            - echo S3 bucket is cleared.
            # Copy dist folder to S3 bucket
            - aws s3 cp dist s3://amer-admin-preprod --recursive
            # STEP: Clear CloudFront cache.
            - aws configure set preview.cloudfront true
            - aws cloudfront create-invalidation --distribution-id EYFGZAHIM4YUY --paths "/*"
artifacts:
    files:
        - '**/*'
    base-directory: 'dist*'
    discard-paths: yes
